import { GraphQLWrapper } from '@aragon/connect-thegraph';
import Vote from './entities/Vote';
import Cast from './entities/Cast';
export default class VotingConnectorTheGraph extends GraphQLWrapper {
    votesForApp(appAddress: string, first: number, skip: number): Promise<Vote[]>;
    onVotesForApp(appAddress: string, callback: Function): {
        unsubscribe: Function;
    };
    castsForVote(voteId: string, first: number, skip: number): Promise<Cast[]>;
    onCastsForVote(voteId: string, callback: Function): {
        unsubscribe: Function;
    };
}
//# sourceMappingURL=connector.d.ts.map