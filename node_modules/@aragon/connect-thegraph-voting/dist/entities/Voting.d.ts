import VotingEntity from './VotingEntity';
import Vote from './Vote';
export default class Voting extends VotingEntity {
    readonly appAddress: string;
    constructor(appAddress: string, subgraphUrl: string, verbose?: boolean);
    votes({ first, skip }?: {
        first?: number | undefined;
        skip?: number | undefined;
    }): Promise<Vote[]>;
    onVotes(callback: Function): {
        unsubscribe: Function;
    };
}
//# sourceMappingURL=Voting.d.ts.map