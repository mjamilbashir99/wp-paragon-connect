import { AppIntent } from '../types';
import App from '../entities/App';
import { TransactionRequestData } from '../transactions/TransactionRequest';
export declare const apmAppId: (appName: string) => string;
export declare const isFullMethodSignature: (methodSignature: string) => boolean;
export declare function validateMethod(destination: string, methodSignature: string, destinationApp: App): AppIntent;
/**
 * Find the method descriptor corresponding to the data component of a
 * transaction sent to `app`.
 *
 * @param  {Object} app App artifact
 * @param  {Object} data Data component of a transaction to app
 * @return {Object|void} Method with radspec notice and function signature, or undefined if none was found
 */
export declare function findAppMethodFromIntent(app: App, transaction: TransactionRequestData): AppIntent | undefined;
//# sourceMappingURL=app.d.ts.map